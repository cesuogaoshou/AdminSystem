<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.admin.module.dict.DictTypeMapper">

    <resultMap id="DictTypeResultMap" type="com.example.admin.module.dict.DictType">
        <constructor>
            <idArg column="id" javaType="java.lang.Long"/>
            <arg column="name" javaType="java.lang.String"/>
            <arg column="code" javaType="java.lang.String"/>
            <arg column="description" javaType="java.lang.String"/>
            <arg column="status" javaType="java.lang.Integer"/>
            <arg column="create_time" javaType="java.time.LocalDateTime"/>
            <arg column="update_time" javaType="java.time.LocalDateTime"/>
        </constructor>
    </resultMap>

    <select id="findAll" resultMap="DictTypeResultMap">
        SELECT id, name, code, description, status, create_time, update_time
        FROM sys_dict_type
        ORDER BY id ASC
    </select>

    <select id="findById" resultMap="DictTypeResultMap">
        SELECT id, name, code, description, status, create_time, update_time
        FROM sys_dict_type
        WHERE id = #{id}
    </select>

    <select id="findByCode" resultMap="DictTypeResultMap">
        SELECT id, name, code, description, status, create_time, update_time
        FROM sys_dict_type
        WHERE code = #{code}
    </select>

    <insert id="insert">
        INSERT INTO sys_dict_type (
            id, name, code, description, status
        ) VALUES (
            #{id}, #{name}, #{code}, #{description}, #{status}
        )
    </insert>

    <update id="update">
        UPDATE sys_dict_type
        SET
            name = #{name},
            code = #{code},
            description = #{description},
            status = #{status}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM sys_dict_type
        WHERE id = #{id}
    </delete>

</mapper>
